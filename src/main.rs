mod forward_chaining;
mod horn_clause;

fn main() {
    println!("Hello, world!");
    /*
    let mut fc = ForwardChaining::new();
    fc.add_clause(vec![-5, 6]);
    fc.add_clause(vec![-3, -4, 5]);
    fc.add_clause(vec![-2, -3, 4]);
    fc.add_clause(vec![-1, -5, 3]);
    fc.add_clause(vec![-1, -2, 3]);
    fc.add_clause(vec![1]);
    fc.add_clause(vec![2]);
    fc.reset_clauses();
    */
}
